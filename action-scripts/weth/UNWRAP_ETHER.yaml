name: "UNWRAP_ETHER"

variables:
  amount: uint256

results:

definitions:
 - Token WETH 0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2
 - Function unwrap WETH withdraw(uint256)

inputs:
 - WETH: amount

actions:
 - WETH unwrap amount

operations:

outputs:
 - ETHER: amount

associations:

description: "Unwrap ${amount:ETHER.decimals} ETH"
