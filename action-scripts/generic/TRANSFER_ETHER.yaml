name: "TRANSFER_ETHER"

summary: "Transfer Ether"

variables:
  to: address
  amount: uint256

results:

definitions:

inputs:
 - ETHER: amount

actions:
 - ETHER to:amount

operations:

outputs:

associations:

description: "Transfer ${amount:ETHER.decimals} ETH to ${to}"
